import SwiftUI

/// Pith Voice — Design System root.
///
/// Generated 2026-05-19 by the `/design-system` skill from SPEC v1.2
/// § Design vector, grounded in 8 reference anchors and 2 anti-anchors
/// documented at `.design-system/pith-voice-2026-05-19/`.
///
/// Editing rules per SPEC § Implementation rules:
/// - No file in the app outside this module may reference a raw hex,
///   raw font size, raw padding number, or raw corner radius.
/// - To change a token, edit it here. SwiftLint `no_raw_*` rules enforce
///   the boundary at lint time. The pre-merge grep documented in SPEC
///   § Acceptance criteria provides a second gate.
enum DS {}
